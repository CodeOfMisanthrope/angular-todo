import {Component, Input} from '@angular/core';

@Component({
  selector: 'app-ui-btn',
  imports: [],
  templateUrl: './ui-btn.html',
  styleUrl: './ui-btn.scss',
})
export class UiBtn {
  // @Input() text!: string;
}
